{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "loki", "uid": "${DS_LOKI}" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(count_over_time({job=\"suricata\"}[1m]))",
          "refId": "A"
        }
      ],
      "title": "Suricata logs volume (fast.log) - events/min",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "loki", "uid": "${DS_LOKI}" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(count_over_time({job=\"suricata\"} |= \"[LAB]\" [1m]))",
          "refId": "A"
        }
      ],
      "title": "LAB alerts/min (fast.log contains [LAB])",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "loki", "uid": "${DS_LOKI}" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 10, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "showTime": true, "wrapLogMessage": true },
      "targets": [
        {
          "expr": "{job=\"suricata\"} |~ \"\\[LAB\\]\"",
          "refId": "A"
        }
      ],
      "title": "Latest LAB alerts (fast.log)",
      "type": "logs"
    },
    {
      "datasource": { "type": "loki", "uid": "${DS_LOKI}" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 10, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "topk(10, sum by (src_ip) (count_over_time({job=\"suricata_eve\"} | json | event_type=\"alert\" | label_format src_ip={{.src_ip}} [10m])))",
          "refId": "A"
        }
      ],
      "title": "Top Source IPs (eve.json, last 10m)",
      "type": "timeseries"
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["mini-soc", "security", "suricata"],
  "templating": {
    "list": [
      {
        "current": { "selected": false, "text": "Loki", "value": "Loki" },
        "hide": 2,
        "name": "DS_LOKI",
        "options": [],
        "query": "loki",
        "refresh": 1,
        "type": "datasource"
      }
    ]
  },
  "time": { "from": "now-30m", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "Security Overview",
  "uid": "mini-soc-security",
  "version": 1
}
